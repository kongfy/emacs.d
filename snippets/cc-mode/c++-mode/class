#name : class ... { ... }
# --
class ${1:Name}
{
  public:
    $1($2);
    virtual ~$1();
  private:
    // types and constants
  private:
    // disallow copy
    $1(const $1 &other);
    $1& operator=(const $1 &other);
    // function members
  private:
    // data members
};